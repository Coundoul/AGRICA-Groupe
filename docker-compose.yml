version: '3'

services:
  springboot-docker-container:
    image: springboot-docker-container
    container_name: springboot-docker-container
    ports:
      - 51.77.132.116:9092:9092
    build:
      context: ./
      dockerfile: Dockerfile